#Example Makefile for the bfs benchmark running with the Graphite Simulator

TARGET := bfs
OBJECTS := $(TARGET).o 

THREADS ?= 256
SIZE ?= 16384
DEGREE ?= 16
APP_FLAGS ?= $(THREADS) $(SIZE) $(DEGREE)

include ../../Makefile.tests
